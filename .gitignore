# Docker Compose file
docker-compose.yml

# Directory for local projects to be analyzed by the agent
/projects/

# Log files generated by the agent
/debugagent/server.log

# IDE-specific files (e.g., for GoLand, VSCode)
/debugagent/.idea/
.vscode/

# Ollama volume data
# This is managed by Docker, but good to ignore in case it appears locally
ollama_data/
